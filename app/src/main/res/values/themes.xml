<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/mainActivityBackground</item>
        <item name="android:statusBarColor">@color/mainActivityBackground</item>
    </style>

    <style name="Theme.SettingsActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/lightStatusBar</item>
        <item name="android:statusBarColor">@color/lightStatusBar</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="ThemeOverlay.AppCompat.DayNight.SearchContainer">
        <item name="android:statusBarColor">@color/lightStatusBar</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:background">@color/mainActivityBackground</item>
        <item name="android:statusBarColor">@color/mainActivityBackground</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.PlaylistMaker.Fullscreen" parent="Base.Theme.PlaylistMaker">
        <item name="android:actionBarStyle">
            @style/Widget.Base.Theme.PlaylistMaker.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PlaylistMaker.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>